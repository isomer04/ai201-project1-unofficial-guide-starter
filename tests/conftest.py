"""
Test configuration.

Pytest settings live in the repo-root pytest.ini. Its `pythonpath = .` puts the
repo root on sys.path so tests here can import the top-level modules (retrieve,
query, config). This file is the home for any shared fixtures added later.
"""
